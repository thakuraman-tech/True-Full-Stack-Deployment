{
  "name": "ethara-full-stack",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npm run build --workspace=client",
    "start": "npm start --workspace=server"
  }
}
